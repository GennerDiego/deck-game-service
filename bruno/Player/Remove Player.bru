meta {
  name: Remove Player
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/games/{{gameId}}/players/{{playerId}}
  body: none
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
}

tests {
  test("should return 204", function() {
    expect(res.status).to.equal(204);
  });
}

docs {
  Removes a player from the game.
  The player's cards are discarded.
}
